# Klipp — Environment variables template
# Copy to .env.local and fill in the values for your environment.

# Base URL of the Klipp backend API (no trailing slash)
# Leave empty to run in dev bypass mode (simulated API responses)
VITE_API_URL=https://api.example.com
